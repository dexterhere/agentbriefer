name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Format, lint, and test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --check

      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Run tests
        run: cargo test

  doctor:
    name: agentbriefer doctor (dogfood)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      # Builds from source rather than letting the action install the
      # published npm package: this job is meant to dogfood the CLI as it
      # exists on this branch (including unreleased flag changes), not the
      # last published release.
      - name: Build agentbriefer
        run: cargo build --release

      - name: Run agentbriefer doctor
        uses: ./.github/actions/doctor
        with:
          binary-path: ${{ github.workspace }}/target/release/agentbriefer
